import { MasterComponent } from "@/modules/core/components/Master";
import { Layout } from "@/modules/shared/components/Layout";

const Home = () => (
	<Layout>
		<MasterComponent />
	</Layout>
);

export default Home;
